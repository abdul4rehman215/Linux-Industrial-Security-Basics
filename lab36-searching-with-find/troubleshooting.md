# 🛠️ Troubleshooting – Lab 36

- No output → verify file existence
- Permission denied errors → use sudo if required
- Incorrect pattern → check wildcard syntax
- -exec not working → ensure proper escaping with \;
- Slow search → limit search path where possible
