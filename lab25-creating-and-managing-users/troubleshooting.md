# 🛠️ Troubleshooting – Lab 25

- useradd command not found → ensure shadow-utils package is installed
- Permission denied → run commands with sudo
- User still exists → check /etc/passwd manually
- Home directory not deleted → use userdel -r
