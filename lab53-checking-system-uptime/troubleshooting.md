# 🛠️ Troubleshooting – Lab 53

- uptime command missing → coreutils not installed
- unexpected high load → check running processes
- misleading load → compare with CPU count
- zero uptime → system was recently rebooted
- inconsistent data → verify system clock
