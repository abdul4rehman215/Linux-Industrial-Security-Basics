# 🛠️ Troubleshooting – Lab 26

- groupadd fails → ensure sudo privileges
- usermod not working → user must already exist
- group not applied → user must re-login for changes to apply
- incorrect group removal → verify with groups username
