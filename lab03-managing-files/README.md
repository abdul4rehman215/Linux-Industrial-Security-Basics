# 🧪 Lab 03: Managing Files

**Environment:** Ubuntu 24.04.1 LTS (Cloud Lab Environment)  
**User:** toor

---

## 🎯 Objectives
- Demonstrate basic file management tasks in a Unix/Linux environment
- Learn how to create, view, and delete files using command-line tools

---

## 📌 Prerequisites
- Basic understanding of operating system concepts
- Access to a Unix/Linux terminal
- Familiarity with CLI basics

---

## 🧩 Lab Tasks & Execution

### 🔹 Task 1: Create an Empty File Using `touch`

**Concept:**  
The `touch` command is used to create empty files or update timestamps of existing files.

---

### 🔹 Task 2: Remove a File Using `rm`

**Concept:**  
The `rm` command permanently deletes files from the filesystem.

⚠️ File deletion using `rm` is irreversible.

---

### 🔹 Task 3: View File Content Using `cat` and `less`

**Concept:**  
- `cat` displays the entire content of a file at once  
- `less` allows paginated viewing and is suitable for larger files

---

## ✅ Conclusion
In this lab, essential file management operations were practiced, including creating files with `touch`, deleting files with `rm`, and viewing file contents using `cat` and `less`.  
These commands are fundamental for daily operations in Unix/Linux environments and are critical skills for system administration and cybersecurity tasks.
