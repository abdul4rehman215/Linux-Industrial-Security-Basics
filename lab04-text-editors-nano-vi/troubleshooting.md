# 🛠️ Troubleshooting – Lab 04

- Cannot type in vi → press `i` to enter Insert Mode
- Cannot exit vi → press Esc, then type `:q!` to force exit
- Nano shortcuts not working → ensure Ctrl key is used correctly
