# 🛠️ Troubleshooting – Lab 44

- pvcreate fails → ensure disk is unused
- lvcreate fails → check VG free space
- mount fails → verify filesystem exists
- lvremove blocked → ensure LV is unmounted
- wrong disk used → STOP immediately to prevent data loss
