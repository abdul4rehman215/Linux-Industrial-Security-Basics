# 🛠️ Troubleshooting – Lab 39

- SSH blocked accidentally → access system via console
- Firewall not enabling → verify ufw package installation
- Rules not applied → recheck syntax and rule order
- Lost connectivity → immediately allow SSH and reload rules
- Conflicting rules → review using ufw status verbose
