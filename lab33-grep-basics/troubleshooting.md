# 🛠️ Troubleshooting – Lab 33

- No output returned → pattern may not exist in the file
- Case-sensitive search failed → use grep -i for case-insensitive search
- Recursive search slow → narrow the directory scope
- Context lines missing → verify -A and -B values
- Permission denied errors → use sudo when required
