# 🛠️ Troubleshooting – Lab 10

- echo $SHELL unchanged after switching → it shows login shell, not current shell
- Unsure of active shell → use echo $0
- Cannot return to previous shell → type exit
